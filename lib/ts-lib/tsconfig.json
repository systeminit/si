{
  // not sure if this should be browser or not?
  "extends": "@si/tsconfig/tsconfig.browser.json",
  "compilerOptions": {
    "composite": true
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts"
  ]
}
