<%- fmt.implServiceAuth(propMethod) %>

let inner = request.into_inner();

let output = <%- fmt.modelName() %>::<%- fmt.modelServiceMethodName(propMethod) %>(&self.db, inner).await?;
info!(?output);

Ok(tonic::Response::new(output))
