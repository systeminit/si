The 


Map model: c.fields.addMap({name: "labels",label: "Labels"})

GraphQL variablesObjectForProperty() : returns -> []

When passing [] back to create the deployment object we get: Field "0" is not defined by type "LabelsRequest".
  message: 'Variable "$properties" got invalid value [{ key: "this key ", value: "this value" }] at "properties.kubernetesObject.spec.template.metadata.labels"; Field "0" is not defined by type "LabelsRequest".',

because LabelsRequest isn't an array

"""Labels"""
input LabelsRequest {
  key: String
  value: String
}

I can 

A) update the prop map code: mapFromArray()
B) format the array as map object when sending the request from the fe
C) update the grapql request to convert arrays of k:v to map objects

siStorable.typeName="kubernetes_deployment_entity"