#import "../fragments/IntegrationInstanceDefault.graphql"

mutation deleteIntegrationInstance($id: ID!) {
  deleteIntegrationInstance(input: { id: $id }) {
    integrationInstance {
      ...IntegrationInstanceDefault
    }
  }
}
