#import "../fragments/WorkspaceDefault.graphql"

query getWorkspacesWithIntegrationInstances {
  getWorkspaces {
    ...WorkspaceDefault
    integrationInstances {
      id
      name
      description
      integration {
        id
        name
        image
      }
    }
  }
}
