apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whiskers.fullname" . }}-test-connection"
  labels:
    {{- include "whiskers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whiskers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
