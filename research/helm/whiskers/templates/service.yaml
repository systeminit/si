apiVersion: v1
kind: Service
metadata:
  name: {{ include "whiskers.fullname" . }}
  labels:
    {{- include "whiskers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whiskers.selectorLabels" . | nindent 4 }}
