import { list } from "./workflow/list";
import { resolve } from "./workflow/resolve";
import { run } from "./workflow/run";
import { history } from "./workflow/history";
import { info } from "./workflow/info";

export const WorkflowService = {
  list,
  resolve,
  run,
  history,
  info,
};
