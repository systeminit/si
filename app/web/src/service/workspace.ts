import { currentWorkspace } from "./workspace/current_workspace";
import { setupWorkspaceWithDefaults } from "./workspace/setup_workspace_with_defaults";

export const WorkspaceService = {
  currentWorkspace,
  setupWorkspaceWithDefaults,
};
