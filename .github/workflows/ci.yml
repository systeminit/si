name: CI
on:
  push:
    paths:
        - ".github/workflows/ci.yml"
        - ".cargo/**"
        - "Cargo.*"
        - "**/*.rs"
        - "rust-toolchain"
        - "rustfmt.toml"
jobs:
  ci-rust:
    runs-on: self-hosted
    steps:
    - name: Git checkout
      uses: actions/checkout@v2
    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable
        override: true
    - name: Execute CI target
      run: make ci
