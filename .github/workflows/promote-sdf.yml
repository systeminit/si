name: promote-sdf

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/promote-image.yml
      - .github/workflows/promote-sdf.yml
      - .cargo/**
      - Cargo.*
      - bin/sdf/**
      - lib/config-file/**
      - lib/dal/**
      - lib/sdf/**
      - lib/si-data/**
      - lib/si-data-nats/**
      - lib/si-data-pg/**
      - lib/si-settings/**
      - lib/telemetry-rs/**
      - lib/veritech-client/**
      - lib/veritech-core/**
      - rust-toolchain
      - rustfmt.toml

jobs:
  promote:
    uses: ./.github/workflows/promote-image.yml
    with:
      component_name: bin/sdf
    secrets:
      SLACKBOT_TOKEN: ${{ secrets.SLACKBOT_TOKEN }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
