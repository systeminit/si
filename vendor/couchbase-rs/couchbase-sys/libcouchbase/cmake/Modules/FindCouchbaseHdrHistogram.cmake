FIND_PATH(HDR_HISTOGRAM_INCLUDES hdr_histogram.h
    HINTS ENV HDR_HISTOGRAM_DIR
    PATH_SUFFIXES include/hdr
    PATHS
	/usr/local)
FIND_LIBRARY(HDR_HISTOGRAM_LIBRARIES NAMES hdr_histogram
    HINTS ENV HDR_HISTOGRAM_DIR
    PATH_SUFFIXES lib
    PATHS
	/usr/local)
IF(HDR_HISTOGRAM_LIBRARIES AND HDR_HISTOGRAM_INCLUDES)
    SET(HDR_HISTOGRAM_FOUND TRUE)
ELSE()
    SET(HDR_HISTOGRAM_FOUND FALSE)
ENDIF()
