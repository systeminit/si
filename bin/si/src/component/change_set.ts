/**
 * Component-specific change set utilities
 *
 * This module provides change set resolution functionality used by component commands.
 * The actual implementation is now in the shared change-set/utils module.
 */

export { resolveChangeSet } from "../change-set/utils.ts";
